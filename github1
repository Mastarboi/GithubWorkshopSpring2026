public boolean sleepIn(boolean weekday, boolean vacation) {
  boolean temp = false;
  
  if (weekday == false)
  {
    temp = true;
  }
  else if (vacation==true)
  {
    temp = true;
  }
  
  return temp;
  
}
